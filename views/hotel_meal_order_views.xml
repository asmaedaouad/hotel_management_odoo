<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Meal Order Tree View -->
    <record id="view_hotel_meal_order_tree" model="ir.ui.view">
        <field name="name">hotel.meal.order.tree</field>
        <field name="model">hotel.meal.order</field>
        <field name="arch" type="xml">
            <tree string="Meal Orders">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="room_id"/>
                <field name="order_date"/>
                <field name="delivery_date"/>
                <field name="delivery_location"/>
                <field name="total_amount" widget="monetary"/>
                <field name="state" widget="badge"/>
            </tree>
        </field>
    </record>

    <!-- Meal Order Form View -->
    <record id="view_hotel_meal_order_form" model="ir.ui.view">
        <field name="name">hotel.meal.order.form</field>
        <field name="model">hotel.meal.order</field>
        <field name="arch" type="xml">
            <form string="Meal Order">
                <header>
                    <button name="action_confirm" string="Confirm" 
                            type="object" class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_prepare" string="Prepare" 
                            type="object" class="oe_highlight"
                            invisible="state != 'confirmed'"/>
                    <button name="action_ready" string="Ready" 
                            type="object" class="oe_highlight"
                            invisible="state != 'preparing'"/>
                    <button name="action_deliver" string="Deliver" 
                            type="object" class="oe_highlight"
                            invisible="state != 'ready'"/>
                    <button name="action_cancel" string="Cancel" 
                            type="object" 
                            invisible="state in ['delivered', 'cancelled']"/>
                    <field name="state" widget="statusbar" 
                           statusbar_visible="draft,confirmed,preparing,ready,delivered"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Order Information">
                            <field name="reservation_id"/>
                            <field name="customer_id"/>
                            <field name="room_id"/>
                        </group>
                        <group string="Delivery Details">
                            <field name="order_date"/>
                            <field name="delivery_date"/>
                            <field name="delivery_location"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Order Lines">
                            <field name="order_line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="meal_type_id"/>
                                    <field name="description"/>
                                    <field name="quantity"/>
                                    <field name="price_unit" widget="monetary"/>
                                    <field name="subtotal" widget="monetary"/>
                                    <field name="currency_id" invisible="1"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Special Instructions">
                            <field name="special_instructions" placeholder="Special instructions for the kitchen..."/>
                        </page>
                        <page string="Internal Notes">
                            <field name="notes" placeholder="Internal notes..."/>
                        </page>
                    </notebook>
                    <group class="oe_subtotal_footer oe_right">
                        <field name="subtotal" widget="monetary"/>
                        <field name="service_charge" widget="monetary"/>
                        <field name="tax_amount" widget="monetary"/>
                        <div class="oe_subtotal_footer_separator oe_inline">
                            <label for="total_amount"/>
                            <field name="total_amount" nolabel="1" class="oe_subtotal_footer_separator" widget="monetary"/>
                        </div>
                        <field name="currency_id" invisible="1"/>
                    </group>
                    <div class="oe_clear"/>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Meal Order Kanban View -->
    <record id="view_hotel_meal_order_kanban" model="ir.ui.view">
        <field name="name">hotel.meal.order.kanban</field>
        <field name="model">hotel.meal.order</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state">
                <field name="id"/>
                <field name="name"/>
                <field name="customer_id"/>
                <field name="room_id"/>
                <field name="state"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title"><field name="name"/></strong>
                                <div class="o_kanban_record_subtitle">
                                    <field name="customer_id"/> - <field name="room_id"/>
                                </div>
                                <div class="o_kanban_record_subtitle">
                                    Delivery: <field name="delivery_date"/>
                                </div>
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left">
                                        <field name="delivery_location"/>
                                    </div>
                                    <div class="oe_kanban_bottom_right">
                                        <field name="total_amount" widget="monetary"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Meal Order Action -->
    <record id="action_hotel_meal_order" model="ir.actions.act_window">
        <field name="name">Meal Orders</field>
        <field name="res_model">hotel.meal.order</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first meal order
            </p>
            <p>
                Manage meal orders for hotel guests.
            </p>
        </field>
    </record>

</odoo>

